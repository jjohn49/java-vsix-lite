// Inert host extension: `runTests` requires an extensionDevelopmentPath, and
// this one deliberately contributes nothing so it cannot influence either
// flavor's results.
function activate() {}
function deactivate() {}
module.exports = { activate, deactivate };
